
/**
 * This legacy layout file is kept for compatibility with earlier code drops.
 * Next.js now uses `layout.tsx`; this component simply renders its children.
 */
export default function LegacyLayout({ children }) {
  return children;
}
